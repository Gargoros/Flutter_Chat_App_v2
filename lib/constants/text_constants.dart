const appTitle = "Let's Talk";

const chatTabText = "Chats";
const statusTabText = "Status";
const callsTabText = "Calls";

const searchHintTextField = "Search or start new chat";

const messageHintText = "Type a message";
