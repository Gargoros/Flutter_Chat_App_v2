import 'package:flutter/material.dart';

const contactsAppBarText = "Select contact";

const selectContactSnackBarText = "This number doesn't exist on this app.";

const contactNameStyle = TextStyle(fontSize: 18);
